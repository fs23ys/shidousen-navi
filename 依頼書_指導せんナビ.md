# 指導せんナビ 実装依頼書

薬剤師本人1人が、調剤・投薬時に患者への服薬指導資料(指導せん)を素早く見つけるための個人用Webアプリを作ってください。
添付の `shidousen-navi-mockup.html` が、機能・見た目・データ構造の完成イメージ(インメモリで動くモックアップ)です。**このモックアップのUI・挙動・データ構造をベースに、Firebaseを使った実際に動くアプリとして実装してください。**

---

## 1. 前提・利用環境

- 利用者は薬剤師本人1人のみ。他者との共有は想定しない。
- 利用端末は薬局PC・自宅PCなど複数端末。同じデータをどこからでも見られる必要がある。
- ネット環境を前提とする(オフライン対応は不要)。

## 2. 技術スタック

- **データ保存**:Firebase(Firestore + 必要であれば Cloud Storage)
- **ホスティング**:**GitHub Pages**(静的サイトとしてビルド・公開する)。フロントエンドはSPA(単一ページアプリ)として作り、`npm run build` 等で生成した静的ファイル一式を GitHub Pages にデプロイする構成とする。
  - GitHub Pagesは静的ファイルの配信のみを行い、サーバーサイド処理は持たない。データの読み書きはブラウザから直接Firebase(Firestore SDK)を呼び出す形になる
  - リポジトリはGitHubで管理し、GitHub Actions等でPagesへの自動デプロイができると望ましい(必須ではない)
  - GitHub Pagesのドメイン(`https://<username>.github.io/<repo>/`)からFirebaseへアクセスすることになるため、FirebaseプロジェクトのAuthorized domains設定にこのドメインを追加する必要がある点に注意
- 認証は本格的なログイン基盤を作らず、**「合言葉」1つによる簡易ゲート**とする(モックアップの `#lockScreen` 参照)
  - Firestoreのセキュリティルールも、この合言葉方式と矛盾しない範囲でできるだけ保護すること(全世界に無制限で読み書きを許可しない)
  - GitHub Pagesで公開する=ソースコード(HTML/JS)は誰でも閲覧できる状態になる。Firebaseの接続情報(APIキー等)がコードに含まれる場合、それ自体は一般的な運用で許容範囲だが、**実際のデータ保護は必ずFirestoreのセキュリティルール側で行う**こと(クライアントコードを隠すことでは保護しない)
- 費用は、個人利用の範囲であれば実質無料(GitHub Pagesは無料、Firestore無料枠、Storageは使うなら無料枠5GBの範囲)を想定。Blazeプランへの登録案内が必要な場合はその旨を明記すること

## 3. データモデル

Firestoreのコレクション設計は以下を基本とする(モックアップのJS変数 `drugs` / `resources` / `sites` / `paperFromHistory` に対応)。

### 3.1 `drugs`(採用医薬品)
| フィールド | 型 | 説明 |
|---|---|---|
| id | string | ドキュメントID |
| name | string | 薬剤名(例:「アムロジピン錠5mg「サワイ」」) |
| jan | string | JANコード |
| gs1 | string | GS1コード |
| yj | string | YJコード(個別医薬品コード) |
| category | string | 薬効分類など |
| maker | string | メーカー名(サイトのメーカー限定表示に使用) |

※ 実際に薬局が保有するExcelには JAN・GS1・YJ のいずれか(または複数)が入っている想定。列名が違っても構わないので、取込時にマッピングできるようにする。

### 3.2 `resources`(登録済み資料=指導せん)
| フィールド | 型 | 説明 |
|---|---|---|
| id | string | ドキュメントID |
| drugId | string | `drugs` のID |
| type | 'web' \| 'paper' | 資料の種類 |
| title | string | 資料タイトル(空欄なら保存時にURLのドメイン名等から自動生成) |
| audience | 'patient' \| 'hcp' | 対象:患者さん向け / 医療関係者向け |
| memo | string | 自由記述メモ |
| url | string | (type='web'の場合)資料のURL、またはサイト入口のURL |
| paperFrom | string | (type='paper'の場合)取り寄せ先(会社名・部署) |
| paperContact | string | (type='paper'の場合)連絡方法(電話番号・Webフォームなど) |

### 3.3 `sites`(検索対象サイト)
| フィールド | 型 | 説明 |
|---|---|---|
| id | string | ドキュメントID |
| name | string | サイト名 |
| domain | string | ドメイン(Google `site:` 検索の対象) |
| required | boolean | 必須サイト(削除不可)かどうか |
| makerOnly | string \| undefined | 指定した場合、`drugs.maker` が一致する薬のときだけ表示 |

初期データとして以下の2件を必須(`required: true`)で登録しておく:
1. くすりのしおり(`rad-ar.or.jp`) — 4章参照、直リンク方式
2. PMDA患者向医薬品ガイド(`pmda.go.jp`) — Google `site:` 検索方式のまま

### 3.4 `paperFromHistory`
`resources` の `paperFrom` に入力された値の履歴(入力補助用のサジェスト候補)。単純な文字列配列で良い。

---

## 4. 主要機能

### 4.1 薬剤のインクリメンタルサーチ
- `drugs` を対象に、入力するたびに絞り込み表示するオートコンプリート
- **カタカナ・ひらがなを区別しない**こと。「あむろ」でも「アムロジピン」がヒットするよう、検索キーを正規化して比較する(カタカナ→ひらがな変換、全角英数字→半角、大文字小文字統一)。モックアップの `toSearchKey()` を参照。
- 薬剤名・分類名の両方を検索対象にする

### 4.2 薬剤選択後の2カラム表示
薬剤を選ぶと、画面に以下を左右に並べて表示する(モックアップの `.cols` 参照)。

**左カラム:サイトでさがす**
- `sites` のうち、`makerOnly` が未設定、または選択中の薬の `maker` と一致するものだけを表示
- 表示されなかった件数があれば「他メーカー限定のサイトを◯件、非表示にしています」と一言添える
- 各サイトはボタン一つで検索結果(または直リンク)を新規タブで開く

**右カラム:登録済み資料**
- 選択中の薬に紐づく `resources` を一覧表示(カード形式)
- 「すべて / 患者さん向けのみ / 医療関係者向けのみ」の絞り込みトグルを持つ
- 各カードに「開く」「編集」「削除」ボタン
- 「＋ 資料を追加」ボタンから登録モーダルを開く

### 4.3 くすりのしおりの直リンク化(重要な非公式仕様)
「くすりのしおり」(RAD-AR、`rad-ar.or.jp`)だけは、Google検索を経由せず、**薬剤選択時に非同期でRAD-ARの検索APIを呼び出し、該当する『しおり』のページへ直接リンクする**こと。

- エンドポイント: `GET https://www.rad-ar.or.jp/siori_register/siori_api/?v=25&r=k&k=t&w=<URLエンコードした薬品名>&g=0`
- レスポンス例: `{"items":[{"id":"37935","name":"アムロジピンOD錠10mg「CH」", ...}, ...]}`
- CORS制限なし(`Access-Control-Allow-Origin: *`)であることをブラウザから確認済み。`*`指定のため、GitHub Pagesのドメインからブラウザで直接呼び出しても問題なく動作するはずだが、実装後は実際のGitHub Pagesドメイン上で必ず動作確認すること
- 候補が複数返るので、**名前を正規化した上でのマッチング**を行うこと:
  1. メーカー名の「」内、適応症の［］【】内、半角全角括弧を除去
  2. 全角英数字を半角に、大文字小文字を統一
  3. 完全一致 → 前方一致 → それでもなければ先頭候補、の優先順で `id` を選ぶ
- 直リンクURL: `https://www.rad-ar.or.jp/siori/search/result?n=<id>`
- API呼び出しに失敗した場合(通信エラー・候補0件など)は、静かに**Google `site:` 検索へのフォールバック**に切り替えること(下記4.4のロジックを流用)。ユーザーにエラーを見せない
- モックアップの `fetchSioriId()` / `normalizeSioriName()` をそのまま参考にしてよい
- **これは非公式なAPIである**ことをコード内にコメントで明記し、将来動かなくなった場合に備えてフォールバックを必ず残すこと

### 4.4 PMDA患者向医薬品ガイド(Google検索フォールバックのみ)
- PMDA側には、くすりのしおりのような直リンク手段は**意図的に採用しない**こと
- 理由:PMDAの検討会資料でも「YJコードからガイドへの直リンクURLを作る仕組み」は未整備であることが明言されている。また、ガイド一覧の静的スクレイピングという代替手段も検討したが、対象パス(`info.pmda.go.jp/downfiles/`)が **robots.txtで明示的にDisallow指定されている**ため、技術的に可能でも実施しないという判断を薬剤師本人と合意済み。この方針を変更しないこと。
- したがって、PMDAガイドは他の一般サイトと同じ**Google `site:` 検索**方式のままとする。

### 4.5 Google `site:` 検索リンクの組み立て(その他のサイト共通)
- クエリ形式: `site:<domain> <薬剤名> ("服薬指導せん" OR "患者向け資料" OR "くすりのしおり" OR "患者向医薬品ガイド")`
- 上記をURLエンコードして `https://www.google.com/search?q=...` を組み立てる
- サイトごとにキーワードを変える機能は不要(全サイト共通の固定キーワードセットでよい、という設計判断済み)

### 4.6 資料の登録・編集
- モーダルで「Web」「紙で取り寄せ」をタブ切り替え
- 「対象」(患者さん向け/医療関係者向け)をタブ切り替えで選択
- Web登録時、URL欄は貼り付けてEnterキーだけで保存できるようにする(タイトル省略可、省略時はURLのドメイン名から仮タイトルを自動生成)
- 前回選んだ「種類」「対象」を次回モーダルを開いたときに引き継ぐ(記憶する)
- 「紙で取り寄せ」の取り寄せ先は、過去に入力した値をサジェスト候補として出す(datalist等)
- 同じモーダルを「編集」ボタンからも開けるようにし、既存データを埋め込んだ状態で開始、保存時は新規追加ではなく上書き更新する

### 4.7 採用医薬品リストの取込(Excel)
- `.xlsx` ファイルをアップロードすると、薬剤名・JAN/GS1/YJコード・分類・メーカー名などの列を読み取り、`drugs` に一括登録する
- 列名の対応付け(マッピング)がある程度柔軟にできると望ましい(列名が完全に一致しなくても、ユーザーがどの列がどの項目かを選べるなど)

### 4.8 データのバックアップ(エクスポート/インポート)
- 「JSONで書き出す」:`drugs` / `resources` / `sites` / `paperFromHistory` をまとめた1つのJSONファイルをダウンロードする
- 「JSONを読み込む」:**マージ方式**で取り込む(上書きしない)
  - 薬剤:YJコードが一致すれば同一とみなす(なければ薬剤名で判定)。新規のみ追加
  - 資料:同じ薬・同じ種類・同じタイトル・同じURL(またはtype='paper'なら取り寄せ先)なら重複とみなしスキップ。それ以外を追加
  - サイト:ドメインが一致すれば重複とみなしスキップ。新規ドメインのみ追加
  - 取り寄せ先履歴:重複しないものだけ追加
  - 取込後、「取り込み完了:薬剤+◯件、資料+◯件、サイト+◯件」のように結果をトースト等で知らせる
- この機能は、後述の「外部で見つけた資材を一括登録する」ワークフロー(4.9)の受け皿として重要なので、フォーマットの互換性を保つこと

### 4.9 (将来利用を想定した)外部一括登録の受け皿
- 薬剤師本人が別途Claude Cowork等を使って「わかりやすい・イラスト付きの患者向け資材」をネット上で探し、`drugs`/`resources`と同じスキーマのJSON(またはCSV)にまとめて出力する運用を想定している
- 4.8のインポート機能がこの受け皿になるので、スキーマ変更時は後方互換性に注意すること

---

## 5. UI/デザインの方針

- 薬局・調剤の現場を思わせる、清潔で落ち着いたトーン(暖色のクリーム系ではなく、淡いセージ系の紙色を基調にした配色を採用済み)
- 資料カードは「索引カード(index card)」のような見た目とし、種類(Web/紙)を左端の色帯で視覚的に区別する
- レスポンシブ対応(2カラム→1カラムへの折り返し)
- 添付モックアップのCSS変数・トークンをできるだけ踏襲すること(独自の再デザインは不要)

---

## 6. 明示的にスコープ外・やらないこと

- ログイン・アカウント管理機能(合言葉のみで良い)
- PMDA患者向医薬品ガイドの自動スクレイピング・API化(4.4参照、robots.txt上の理由により見送り)
- 検索対象サイトごとに異なるキーワードを設定する機能(現状は全サイト共通キーワードでよい)
- オフライン対応

---

## 7. 添付ファイル

- `shidousen-navi-mockup.html` … 本依頼書の内容を反映したインメモリ動作のモックアップ(単一HTMLファイル)。実装時の挙動・デザインのリファレンスとして使用してください。

---

## 8. 未確定・実装時に確認してほしい点

- Excel取込時の列名マッピングの具体的なUI(自動判定にするか、手動で列を選ばせるか)
- Firestoreセキュリティルールの詳細な書き方(合言葉との連携方法含む)
- くすりのしおりAPIが将来的に仕様変更・アクセス制限された場合の挙動確認方法(定期的な死活監視は不要だが、フォールバックが正しく機能するかのテストは実施してほしい)
